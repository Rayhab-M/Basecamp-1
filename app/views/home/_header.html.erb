<nav class="navbar navbar-expand-lg navbar-dark bg-dark fs-6">
  <div class="container-fluid"  style="font-size: 0.8rem;">
  <%= link_to 'BASECAMP', root_path, class:"navbar-brand"  %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <% if user_signed_in?%>
      <li class="nav-item">
        <%= link_to 'Project', projects_path, class:"nav-link" %>
        </li>

              <li class="nav-item">
              <%= link_to 'New Project', new_project_path, class:"nav-link" %>
              </li>

              <li class="nav-item">
              <%= link_to 'Edit profile', edit_user_registration_path, class:"nav-link" %>
              </li>

              <% if current_user.admin? %>
                <%= link_to 'Manage Users', users_path, class:"nav-link" %>
                <%= link_to 'Manage Projects', projects_path, class:"nav-link" %>
              <% end %>

        </ul>
        <form class="d-flex fs-6" role="search" style="font-size: 0.875rem; padding: 5px;" action="/search" method="get">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search" name="search" id="search-input" style="font-size: 0.875rem; padding: 0.375rem;">
        <button class="btn btn-outline-primary me-2 fs-6" type="submit" style="font-size: 0.875rem; padding: 0.375rem 0.75rem;">Search</button>
        <%= link_to 'LogOut', destroy_user_session_path, 
                    data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, 
                    class: 'btn btn-danger fs-6' 
        %>
      </form>
    </div>
  </div>
  <% end %>
</nav>
